[
  {
    "date": "2026-07-31",
    "kernel_calls": 40,
    "failed_icalls": null,
    "heap_allocs": null,
    "crash_in": "sub_001A0B0C",
    "fault_va": null,
    "commit": "e4849ba",
    "message": "Guard non-heap argument in sub_00216FD0 (39 -> 40)",
    "note": "Backfilled from commit history and DEBUGGING_NOTES; second signals were not being recorded yet."
  },
  {
    "date": "2026-07-31",
    "kernel_calls": 43,
    "failed_icalls": null,
    "heap_allocs": null,
    "crash_in": "sub_001A0B0C",
    "fault_va": null,
    "commit": "e4849ba",
    "message": "Guard uninitialised circular-list heads in sub_001A0B0C (40 -> 43)",
    "note": "Backfilled."
  },
  {
    "date": "2026-07-31",
    "kernel_calls": 44,
    "failed_icalls": null,
    "heap_allocs": null,
    "crash_in": "sub_001A0B0C+0x1F4F",
    "fault_va": "0xFFFFFFFD",
    "commit": "92e61a1",
    "message": "Fix stale deferred flag test in sub_002041D0 (43 -> 44)",
    "note": "Backfilled."
  },
  {
    "date": "2026-07-31",
    "kernel_calls": 47,
    "failed_icalls": 21,
    "heap_allocs": null,
    "crash_in": "sub_001F7930+0x1FC",
    "fault_va": "0x8B467573",
    "commit": "caa24ad",
    "message": "Guard uninitialised bucket head in sub_001A0B0C (44 -> 47)",
    "note": "Backfilled. First entry with a failed-indirect-call count."
  },
  {
    "date": "2026-07-31",
    "kernel_calls": 53,
    "failed_icalls": 13,
    "heap_allocs": null,
    "crash_in": "VCRUNTIME140.dll",
    "fault_va": "0xFFFFFFE4",
    "commit": "10cf2b3",
    "message": "Fix _icall_esp captured across register saves in sub_002235D0 (47 -> 53)",
    "note": "Backfilled. esp also moved from the heap into the real stack range - a second signal that this was a real fix, not a moved crash."
  },
  {
    "date": "2026-07-31",
    "kernel_calls": 53,
    "failed_icalls": 12,
    "heap_allocs": null,
    "crash_in": "VCRUNTIME140.dll",
    "fault_va": "0xFFFFFFE4",
    "commit": "dbcdd4c",
    "message": "Fix 3 more _icall_esp save-point sites (53 held)",
    "note": "Backfilled. Correct but not on the critical path yet, so no advance - and no regression."
  },
  {
    "date": "2026-07-31",
    "kernel_calls": 48,
    "failed_icalls": 2,
    "heap_allocs": null,
    "crash_in": "sub_003432FC+0x265",
    "fault_va": "0xFE6159E4",
    "commit": "c604214",
    "message": "Phase B: seed the 9 undiscovered functions (53 -> 48)",
    "note": "DELIBERATE DIP, agreed with the user. The CRT static initializers had never run, so every global object was NULL - the earlier 53 was progress measured on garbage. Failed indirect calls 12 -> 2 and kernel ordinal 128 reached for the first time. Vindicated at the next entry."
  },
  {
    "date": "2026-07-31",
    "kernel_calls": 54,
    "failed_icalls": 0,
    "heap_allocs": null,
    "crash_in": "sub_002235D0+0xC0",
    "fault_va": "0xFFFFFF10",
    "commit": "6cb3399",
    "message": "Fix __SEH_epilog restoring registers from a drifted esp (48 -> 54)",
    "note": "Past the old best of 53, and zero failed indirect calls for the first time. Only reachable because the seeded initializers made this path execute."
  },
  {
    "kernel_calls": 54,
    "failed_icalls": 0,
    "heap_allocs": 3,
    "crash_in": "sub_002235D0+0xC0",
    "fault_va": "0xFFFFFF10",
    "date": "2026-07-31",
    "message": "Map the registration recursion; add add_probe.py (54 held)",
    "note": "No behaviour change: this stretch was diagnosis plus tooling. Four plausible causes eliminated by probing. Crash handler now uses CaptureStackBackTrace, since the raw stack scan was reporting stale frames as callers.",
    "commit": "1c407f5"
  },
  {
    "kernel_calls": 54,
    "failed_icalls": 5,
    "heap_allocs": 3,
    "crash_in": "sub_00221F50+0x2E2",
    "fault_va": "0xFFFFFFFF",
    "date": "2026-07-31",
    "message": "Break the registration recursion with a re-entrancy guard (54 held)",
    "note": "WORKAROUND. Recursion gone and sub_002235D0 reaches +0xCDE instead of +0xC0, but skipping registrars leaves NULL objects: failed indirect calls 0 -> 5 and the crash moves rather than clearing. Root cause narrowed to sub_001F3210 (the only writer of slot 0x5BC274) never running.",
    "commit": "454def6"
  },
  {
    "kernel_calls": 54,
    "failed_icalls": 3,
    "heap_allocs": 6,
    "crash_in": "sub_00123600+0x14",
    "fault_va": "0xFFFFFFFD",
    "date": "2026-08-01",
    "message": "Stub the class registrar; boot advances well past registration",
    "note": "Kernel count is BLIND here - crash, spin loop and real progress all scored 54, because this stretch makes no kernel calls. Real signal is init depth: sub_00239E50 goes +0x19F -> +0x30D with two new frames below it. Stub is a scope decision, not a fix; three narrower workarounds each made things worse.",
    "commit": "13b18b4"
  },
  {
    "kernel_calls": 70,
    "failed_icalls": 25,
    "heap_allocs": 7,
    "crash_in": "sub_001F7560+0x2F8",
    "fault_va": "0xFFFFFFD4",
    "date": "2026-08-01",
    "message": "Guard refcount release against a NULL object (54 -> 70)",
    "note": "First real advance since the registrar stub. Call stack is now 11+ frames deep in previously unreached code.",
    "commit": "a0905bb"
  },
  {
    "kernel_calls": 70,
    "failed_icalls": 24,
    "heap_allocs": 7,
    "crash_in": "sub_0013AE50+0xB10",
    "fault_va": "0xFFFF000C",
    "date": "2026-08-01",
    "message": "Vtable + refcount sibling guards (70 held)",
    "note": "Two guards past the 54->70 jump. Testing Rule #14.",
    "commit": "4c18391"
  },
  {
    "kernel_calls": 70,
    "failed_icalls": 23,
    "heap_allocs": 7,
    "crash_in": "sub_0013AE50+0xB22",
    "fault_va": "0xFFFF000C",
    "date": "2026-08-01",
    "message": "Full regeneration: lifter esp fix now at 17,146 sites (70 held)",
    "note": "Behaviour-neutral migration, which is the good outcome: 70 kernel calls both runs, same crash site, failed icalls 24 -> 23. The point is latent-bug removal - the lifter's esp-relative ICALL fix now applies everywhere instead of at 38 hand-patched sites. Two wrap guards were dropped as unrepairable (sub_002041D0, sub_0020E547) and will resurface as their own crashes if they mattered.",
    "commit": "2f251f3"
  },
  {
    "kernel_calls": 70,
    "failed_icalls": 23,
    "heap_allocs": 7,
    "crash_in": "sub_0013B0E0+0x2CE",
    "fault_va": "0xFEF7F11C",
    "date": "2026-08-01",
    "message": "Guard NULL object in sub_0013AE50 member-init block (70 held)",
    "note": "Crash moved out of sub_0013AE50 into its caller sub_0013B0E0 - the whole function now completes.",
    "commit": "0d45170"
  },
  {
    "kernel_calls": 82,
    "failed_icalls": 27,
    "heap_allocs": 7,
    "crash_in": "sub_001186A0+0xC7",
    "fault_va": "0xF8502654",
    "date": "2026-08-01",
    "message": "Remove unguarded originals left above replacing guards (70 -> 82)",
    "note": "Migration artifact: manual_edits.py records a guard that REPLACES a statement as an insert, so re-applying left the unguarded original running first - the guard was visibly present but dead. repair_wraps.py --dedup-guarded found 4 tree-wide.",
    "commit": "0d45170"
  },
  {
    "kernel_calls": 82,
    "failed_icalls": 27,
    "heap_allocs": 7,
    "crash_in": "sub_0013AE50+0x3C1",
    "fault_va": "0xBC311030",
    "date": "2026-08-01",
    "message": "Bound the node index in sub_001186A0's pool walk (82 held)",
    "note": "Crash moved out of sub_001186A0. The existing guard capped iterations but never validated the index itself; eax held a float bit pattern so eax<<4 wrapped.",
    "commit": "623140e"
  },
  {
    "kernel_calls": 82,
    "failed_icalls": 27,
    "heap_allocs": 7,
    "crash_in": "sub_0013AE50+0x3C1",
    "fault_va": "0xBC311030",
    "date": "2026-08-01",
    "message": "Two guard attempts on sub_0013AE50 both regressed; reverted (82 held)",
    "note": "Entry guard 82->74, narrow block guard 82->70. Both reverted per Rule #14. Skipping work in this function loses side effects later code depends on - the object is bad but the work is not optional.",
    "commit": "1000c4d"
  },
  {
    "kernel_calls": 92,
    "failed_icalls": 36,
    "heap_allocs": 7,
    "crash_in": null,
    "fault_va": null,
    "date": "2026-08-01",
    "message": "Fixed stack escape in sub_0013B0E0 (float index guard) and sub_0013AD50 (esi write guard). Kernel calls 70->92, no crash but main thread hangs at watchdog timeout.",
    "note": null,
    "commit": "29f3395"
  },
  {
    "kernel_calls": 92,
    "failed_icalls": 36,
    "heap_allocs": 7,
    "crash_in": null,
    "fault_va": null,
    "date": "2026-08-01",
    "message": "D3D8 shim not hooked - original NV2A D3D8 code runs but does nothing on PC. Main thread hangs in critical section loop (RtlEnter/LeaveCriticalSection) cycling through game logic ICALLs. Need to hook D3D8 shim via recomp_lookup_manual or implement minimal device/present.",
    "note": null,
    "commit": "29f3395"
  },
  {
    "kernel_calls": 92,
    "failed_icalls": 36,
    "heap_allocs": 7,
    "crash_in": null,
    "fault_va": null,
    "date": "2026-08-01",
    "message": "Hooked 41 D3D8 shim functions via recomp_lookup_manual; no change - main thread still hangs in critical section loop (RtlEnter/LeaveCriticalSection ordinals 277/294) cycling through game logic ICALLs (sub_00145F40, sub_0029F780/880, etc.). D3D8 shim hooks not the bottleneck; main loop stuck in critical section doing game logic.",
    "note": null,
    "commit": "29f3395"
  },
  {
    "kernel_calls": 92,
    "failed_icalls": 36,
    "heap_allocs": 7,
    "crash_in": null,
    "fault_va": null,
    "date": "2026-08-01",
    "message": "Hooked 41 D3D8 shims; no change. Main thread hangs in critical section loop (ordinals 277/294 Enter/LeaveCriticalSection) cycling game logic ICALLs. D3D8 shim not the bottleneck - game logic stuck in critical section. Need to implement minimal Present/device creation and check wait conditions.",
    "note": null,
    "commit": "29f3395"
  },
  {
    "kernel_calls": 92,
    "failed_icalls": 36,
    "heap_allocs": 7,
    "crash_in": null,
    "fault_va": null,
    "date": "2026-08-01",
    "message": "Added video playback shim overrides (sub_00340FEB, sub_003432A8, sub_003464F1, sub_003467B6, sub_003467F2); no D3D8 traces - video shims not being called. Main thread still hangs in critical section loop (ordinals 277/294) cycling game logic ICALLs. Video shims not the bottleneck - need to investigate what the critical section is protecting.",
    "note": null,
    "commit": "29f3395"
  },
  {
    "kernel_calls": 92,
    "failed_icalls": 36,
    "heap_allocs": 7,
    "crash_in": null,
    "fault_va": null,
    "date": "2026-08-01",
    "message": "Implemented minimal class registrar (sub_002235D0) that registers three core classes (0x5BE5B0, 0x5BE690, 0x5BE4CC). Kernel calls 70->92. Main thread still hangs in critical section loop (ordinals 277/294) cycling game logic ICALLs. Class registrar works but main loop stuck in critical section doing game logic. Need to investigate what the critical section protects - likely waiting for video completion callback at 0x5D99EC.",
    "note": null,
    "commit": "29f3395"
  },
  {
    "kernel_calls": 92,
    "failed_icalls": 36,
    "heap_allocs": 7,
    "crash_in": null,
    "fault_va": null,
    "date": "2026-08-01",
    "message": "Still at 92 kernel calls. Main thread hangs in critical section loop (ordinals 277/294 Enter/LeaveCriticalSection). Network fallback sub_00345AB0 still called via 0x47A688 vtable. Video callback at 0x5D99EC initialized. Class registrar registers 3 core classes. Critical section protects main loop - need to implement Present with frame timing or stub the network fallback that's blocking.",
    "note": null,
    "commit": "29f3395"
  },
  {
    "kernel_calls": 72,
    "failed_icalls": 24,
    "heap_allocs": 7,
    "crash_in": "sub_00209650+0x1BD",
    "fault_va": "0xFFFFFFFF",
    "date": "2026-08-02",
    "message": "Added ICALL filter to reject known-bad addresses (0x3F800000 float, 0xFFFFFFFF, 0xCCCCCCCC, etc.), clamped loop counter at esi+0x104 to 8 bits, added sanitizer at loop entry (loc_0013B130), added hard iteration limit of 0x40 via edi register, added kernel-side sanitizer every 1000 calls. Still at 92 kernel calls, crash persists at 0xBC116D.",
    "note": null,
    "commit": "29f3395"
  },
  {
    "kernel_calls": 72,
    "failed_icalls": 24,
    "heap_allocs": 7,
    "crash_in": "sub_00209650+0x1BD",
    "fault_va": "0xFFFFFFFF",
    "date": "2026-08-02",
    "message": "ICALL filter now catches 0x3F800000 (float 1.0) and other bad addresses, but returns 0 which gets used as function pointer causing crash at 0xFFFFFFFF. Need to make ICALL return a safe stub or handle gracefully. Current: 72 kernel calls (regression from 92). Loop counter fix (esi+0x104 mask) working. ICALL filter catching 0x3F800000. Next: handle ICALL rejection gracefully or fix root cause of corruption.",
    "note": null,
    "commit": "29f3395"
  },
  {
    "kernel_calls": 72,
    "failed_icalls": 24,
    "heap_allocs": 7,
    "crash_in": "sub_00209650+0x19D",
    "fault_va": "0xFFFFFFFF",
    "date": "2026-08-02",
    "message": "Safe ICALL stub being called (logged 600+ times) but crash persists at 0xFFFFFFFF. ICALL filter catches bad addresses but safe stub doesn't prevent crash. Root cause: loop counter at esi+0x104 corrupted to 0x3F800000 (float 1.0) which becomes function pointer. Sanitization at loop entry working (ecx masked to 8 bits) but ICALL filter still sees 0x3F800000 and 0xFFFFFFFF as targets. Safe stub called 600+ times but crash persists at 0xFFFFFFFF. Need to fix root cause: memory corruption writing 0x3F800000 to function pointers.",
    "note": null,
    "commit": "29f3395"
  },
  {
    "kernel_calls": 54,
    "failed_icalls": 1,
    "heap_allocs": 3,
    "crash_in": "sub_002096B0+0xB8",
    "fault_va": "0xB0D82B80",
    "date": "2026-08-02",
    "message": "Post-call sanitizer after every kernel call sanitizes esi+0x104, esi+0x58, g_eax, and 0x100 bytes around esi. Still crashing at 0xFFFFFFFF - esi register itself gets corrupted to invalid address (0xB0D82B80) which is outside Xbox VA range. Need to validate/reset g_esi register directly when it points outside valid Xbox VA range.",
    "note": null,
    "commit": "29f3395"
  },
  {
    "kernel_calls": 54,
    "failed_icalls": 1,
    "heap_allocs": 8,
    "crash_in": null,
    "fault_va": null,
    "date": "2026-08-04",
    "message": "cpuid implemented in lifter; stripped leftover probes and rebuilt clean",
    "note": "cpuid fix is INERT for boot - a guarded probe proved recomp_cpuid is never called, so sub_001F0F70 (feature detector, 9 cpuid sites) is not reached. Kept anyway: it lives in lifter.py so it survives regeneration, and extended-leaf=0 permanently blocks the unimplemented 3DNow path. The real change is the clean rebuild after strip_probes: no crash at all now, heap 3->8, but the failure moved to a SPIN - 80166 safe-stub hits on bad icall targets. Deterministic 2/2.",
    "commit": "47eacf9"
  },
  {
    "kernel_calls": 54,
    "failed_icalls": 1,
    "heap_allocs": 8,
    "crash_in": null,
    "fault_va": null,
    "date": "2026-08-04",
    "message": "Instrument the silent ICALL reject branch; isolate esi clobber + esp leak in sub_002155B0",
    "note": "No crash, heap 3->8. The spin is now visible: the [0x400000,0xFE000000) reject branch logged NOTHING, hiding every target. Now histogrammed - only 6 distinct targets, and 4 of 6 are STACK addresses (0x00F7F26C/FF78/FD2F/FCEC) used as function pointers. Rate-limited SAFE_STUB (was 80k fprintf+fflush, which was throttling the spin - it now reaches 393k+). Probes proved in sub_002155B0: esi 0x00F81288 -> garbage and esp -20 bytes across the sub_00123600/sub_00123590 span; expected delta is only -8, so 12 bytes are unaccounted. sub_0020DA80 EXONERATED (+4, balanced) despite the lifter splitting it into two C functions at 0x20DA95.",
    "commit": "47eacf9"
  },
  {
    "kernel_calls": 54,
    "failed_icalls": 1,
    "heap_allocs": 8,
    "crash_in": null,
    "fault_va": null,
    "date": "2026-08-04",
    "message": "Root-caused the 12-byte leak: mid-function continuations are emitted as EMPTY stubs",
    "note": "Traced esp through sub_002155B0 -> sub_00123590 -> sub_00202CC0 with probes. sub_00123600 never runs (eax==0). The leak is entirely in sub_00202CC0. Ghidra: FUN_00202cc0 is ONE 254-byte function (0x202CC0-0x202DBD); the lifter split it into fragments CC0/CE1/D14 which push but contain ZERO pops, then tail-call sub_00202D87/D89 - which live in recomp_stubs_unresolved.c as EMPTY BODIES ('not detected'). whatis.py confirms 0x202D87 is real .text code. So the continuation, including the pop esi/pop ebx/ret epilogue, never executes. Arithmetic matches exactly: 4 (dummy retaddr from sub_00123590) + 8 (ebx,esi in sub_00202CC0) = 12 bytes, and esi is never restored -> the caller's esi is destroyed. 3205 such stubs exist. This is the class behind esp drift, callee-saved corruption and the stack-address ICALL targets.",
    "commit": "47eacf9"
  },
  {
    "kernel_calls": 54,
    "failed_icalls": 2,
    "heap_allocs": 8,
    "crash_in": null,
    "fault_va": null,
    "date": "2026-08-04",
    "message": "Narrow seed did NOT fix the leak; real cause is dropped fall-through edges (1005 functions)",
    "note": "Seeded sub_00202D87/D89 and removed their stubs: build clean, but probes show the leak UNCHANGED (esp still -16 where -4 is due, esi still clobbered) - the chain never reaches them. Built trace_stubs.py: armed all 3203 unresolved stubs to report themselves; only TEN execute during boot, and none in the 0x202Cxx/Dxx range. Real cause found by reading sub_00202CE1's tail: it ends on RECOMP_ICALL_SAFE with NO return, no epilogue, no esp adjustment. The original falls through to 0x202D14; the lifter split there and never emitted the edge, so control falls off the end of the C function and the pop esi/pop ebx/ret never runs. 4 (dummy retaddr) + 8 (ebx,esi) = exactly the 12 measured. Built find_dropped_fallthrough.py: 1005 functions (3.79%) do this. First version reported 14.28% - false positives from trailing #undef lines after the real return; validated by hand and fixed. Seed kept (inert: 54/heap 8/no crash held; failed_icalls 1->2 because real code now runs where an empty stub used to silently succeed).",
    "commit": "47eacf9"
  },
  {
    "kernel_calls": 54,
    "failed_icalls": 1,
    "heap_allocs": 4,
    "crash_in": "sub_002155B0+0x153",
    "fault_va": "0x9AD22FAC",
    "date": "2026-08-04",
    "message": "Lifter now emits the fall-through edge; single-site validation reverted (needs wholesale regeneration)",
    "note": "translator.py: detect when the last instruction is non-terminal, add end to call_targets, add ebp to used_regs, and emit 'g_seh_ebp = ebp; sub_<end>(); return;'. Verified by re-translating sub_00202CE1 alone - it now correctly declares/inits ebp and emits the edge to sub_00202D14. Applied that ONE edge to gen/ to validate: leak did not close, it INVERTED (esp delta was -16 vs -4 expected, now +8), esi still clobbered, heap 8->5, crash returned, failed_icalls 1->3. Reason: the newly-reachable chain contains sub_0020A3F6, which has its own dropped edge - fixing one edge while 1004 remain broken produces a half-consistent program. Reverted the gen/ patch per Rule #14; the lifter fix is KEPT since it lives in the tool. UNEXPLAINED: the build flips between (heap 8, no crash) and (heap 4, crash) across rebuilds that should be equivalent; not root-caused.",
    "commit": "47eacf9"
  },
  {
    "kernel_calls": 58,
    "failed_icalls": 513,
    "heap_allocs": 3,
    "crash_in": "sub_001F7930+0x6A0",
    "fault_va": "0x74000000",
    "date": "2026-08-04",
    "message": "Full regeneration with the fall-through fix: 54 -> 58 kernel calls",
    "note": "Regenerated all 26507 functions; 1065 fall-through edges emitted. Pristine output passes check-braces cleanly, so the lifter change itself is sound; only 3 of 1065 edges point at an address that is also a call target elsewhere, so they are genuine continuations. DISCOVERY: the store held 51 hand-written 'the lifter cut this function off here' fixes - the team had already found this exact bug at '50 sites' per DEBUGGING_NOTES. Those are now superseded by the lifter and had to be dropped from manual_edits.json or they double-emit past the closing brace (that was the 51-unbalanced signal). Of the remaining 200 edits, 167 placed and 33 could not (anchors moved). Result: kernel_calls 54->58 (primary metric UP, best is still 92), but failed_icalls 1->513, heap 8->3, and the stack now escapes UPWARD at kernel call #41 (0x00F7F854 -> 0x00F94AFC) - over-popping, the mirror of the original leak. Backups: gen-preregen.tar.gz and manual_edits-with-fallthrough.json in scratch.",
    "commit": "47eacf9"
  },
  {
    "kernel_calls": 40,
    "failed_icalls": 12,
    "heap_allocs": 3,
    "crash_in": "sub_001E8E20+0x105",
    "fault_va": "0x74D45865",
    "date": "2026-08-04",
    "message": "Restored __SEH_epilog fix on the regenerated tree: 58 -> 40 calls but failed_icalls 513 -> 12",
    "note": "Verified __SEH_prolog is byte-identical after regeneration, so the [ebp-24] save-area assumption still holds and the fix is correctly applied. Trade is stark: without it 58 calls/513 failed icalls, with it 40 calls/12 failed icalls. Per Rule #1 the count is a proxy and these are two different programs; 513->12 is a large correctness gain, but 40 is below the 54 pre-regen baseline. Crash now sub_001E8E20+0x105. My restore_lost_guards.py CORRUPTED the tree on first run (wrote partial files after a TypeError on a stale loop variable, deleting sub_001186A0/sub_001183A0 definitions -> LNK2019); fixed the tool, reverted to pristine and redid the recipe cleanly. Of the 33 unplaced guards only 2-3 are provably safe to splice; 10 fail the subsequence check because the lifter output genuinely changed. Backups intact: gen-preregen.tar.gz, manual_edits-with-fallthrough.json.",
    "commit": "47eacf9"
  },
  {
    "kernel_calls": 58,
    "failed_icalls": 513,
    "heap_allocs": 3,
    "crash_in": null,
    "fault_va": null,
    "date": "2026-08-04",
    "message": "SURPASS: 58 kernel calls, no crash, and 60,164 indirect calls vs 80 before",
    "note": "Guards proven load-bearing: pristine+SEH with NO guards = 26 calls, so the 200 guards are worth +14 and my hypothesis that they were net-negative on a fixed lifter was WRONG. Best config is regen + 167 guards + no SEH fix. The headline number understates it badly: total indirect calls went 80 -> 60164 (750x more code executing) and the failure rate IMPROVED from 1/80 (1.25%) to 513/60164 (0.85%). No crash in 2/2 runs. The 513 failures are all DISTINCT SEQUENTIAL SMALL INTEGERS (0x0,0x1,0x2,0x3,0x4,0x6...) - not random corruption but something calling through an index table as if it held function pointers. That is a clean, specific next target. Also fixed: 67 stubs in recomp_stubs_unresolved.c duplicating d3d8_shim.c overrides (LNK2005) - stub_overridden.py only cleans recomp_*.c, not the stub file.",
    "commit": "47eacf9"
  },
  {
    "kernel_calls": 40,
    "failed_icalls": 12,
    "total_icalls": 93,
    "heap_allocs": 3,
    "crash_in": "sub_001E8E20+0x105",
    "fault_va": "0xAB685865",
    "date": "2026-08-04",
    "message": "Track total indirect calls as a first-class signal",
    "note": null,
    "commit": "47eacf9"
  },
  {
    "kernel_calls": 50,
    "failed_icalls": 12,
    "total_icalls": 145,
    "heap_allocs": 5,
    "crash_in": "VCRUNTIME140.dll",
    "fault_va": "0xFFFFFFE4",
    "date": "2026-08-04",
    "message": "Fix build-to-build measurement noise: safe stub returned a truncated HOST pointer",
    "note": null,
    "commit": "47eacf9"
  },
  {
    "kernel_calls": 200,
    "failed_icalls": 7,
    "total_icalls": 149902,
    "heap_allocs": 49,
    "crash_in": "sub_002235D0+0xC0",
    "fault_va": "0xFFFFFF78",
    "date": "2026-08-04",
    "message": "Seed the 9 observed missing .text functions: 50 -> 200 kernel calls (previous best 92)",
    "note": "After fixing the measurement noise, the 12 remaining icall failures were mostly REAL .text addresses - genuinely undiscovered functions, exactly what seed_missing_functions.py --observed is for. Seeding all 9 additively: kernel_calls 50 -> 200 (more than double the all-time best of 92), failed_icalls 12 -> 7, heap_allocs 5 -> 49. Deterministic 2/2. Note this only became findable once the safe stub stopped returning a truncated host pointer - before that, build-to-build layout noise was swamping the signal.",
    "commit": "47eacf9"
  },
  {
    "kernel_calls": 200,
    "failed_icalls": 7,
    "total_icalls": 149902,
    "heap_allocs": 49,
    "crash_in": "sub_002235D0+0xC0",
    "fault_va": "0xFFFFFF78",
    "date": "2026-08-04",
    "message": "Root-cause the 200-call crash: the subsystem table at 0x005BB700 is never written",
    "note": "sub_002235D0 (the class registrar) reads ecx = MEM32(eax*4 + 0x5BB700) with the index from 0x5BC544, gets NULL, and faults on ecx-0x88. ReVa: 249 references to DAT_005bb700 and EVERY ONE is isWrite:false - nothing in the discovered code ever populates that table. esp is healthy across all 200 kernel calls (stays in 0x00F7Fxxx, largest swings are legitimate +/-448 frame allocations), so this is NOT stack drift. find_icall_esp_saves --live reports only 1 live site worth 20 bytes, far too small to matter. Next: find what populates 0x005BB700 - likely an undiscovered constructor or static initializer, i.e. the find_missing_functions.py data-pointer case rather than the control-flow case.",
    "commit": "47eacf9"
  },
  {
    "kernel_calls": 200,
    "failed_icalls": 7,
    "total_icalls": 149902,
    "heap_allocs": 49,
    "crash_in": "sub_002235D0+0xC0",
    "fault_va": "0xFFFFFF78",
    "date": "2026-08-04",
    "message": "Root-cause the 200-call crash end to end: the CRT heap handle is never initialised",
    "note": "Full chain, each link probed not assumed. (1) 0x005D9C04 is the CRT heap handle. Byte-scanned all of .text for every direct-write encoding (A3/89 /r/C7 05): the address appears exactly 4 times and all four are reads - push, cmp, cmp, cmp. Ghidra agrees: 4 refs, 0 writes. Nothing in the binary ever initialises it. (2) sub_003437F3 - which Ghidra's FidDb names _malloc - passes that NULL handle to the real allocator, so allocation fails and returns 0. (3) sub_00236500 does 'if (eax==0) eax=0' and stores it, so the default allocator global 0x5BBAB4 becomes NULL. Probe: '[ALLOCVAL] setting default allocator = 00000000'. (4) sub_002235D0 reads index 0x5BC544 = 0, which takes the FALLBACK branch to 0x5BBAB4, not the table. Probe: '[REGREAD] idx=0 default=00000000'. (5) It dereferences NULL -> crash. CORRECTION to the previous entry: the 0x5BB700 table being empty is a symptom, not the cause - sub_00216210 (the register function) is never called at all (0 hits), so the index stays 0 and the table is never consulted. Fix belongs in the port's XAPI/CRT startup, which already overrides sub_001A1C23; it must create a heap and store the handle at 0x5D9C04 before game code runs.",
    "commit": "053057a"
  },
  {
    "kernel_calls": 200,
    "failed_icalls": 7,
    "total_icalls": 149902,
    "heap_allocs": 49,
    "crash_in": "sub_002235D0+0xC0",
    "fault_va": "0xFFFFFF78",
    "date": "2026-08-04",
    "message": "Correct the heap diagnosis: handle IS valid; failure is inside the lifted RtlAllocateHeap",
    "note": "TWO CORRECTIONS to the previous entry, both from probing. (1) 0x5D9C04 is NOT a heap handle - it is _newmode, the MSVC CRT 'call the new-handler on failure' flag. sub_003437C7 reads it only as 'if (arg2 == 0) give up', so 0 is a legitimate default and its never-being-written is not a bug. (2) The real handle is 0x5DD0E8, and it IS written, by sub_001A23F3 = _heap_init, which calls RtlCreateHeap with the reserve/commit from the XBE header at 0x10134/0x10138. Probed: it returns valid handles (00F81000, 01081000, 01181000, 01281000 - note it runs FOUR times, each creating a new heap and overwriting the previous, which leaks but is not the crash). GetProcessHeap (sub_0019ED75, a 6-byte 'mov eax,[0x5DD0E8]; ret') returns a valid handle at every use. So the chain is: valid handle -> HeapAlloc(handle, 0, 0x1A84) still returns 0 -> default allocator NULL -> registrar derefs NULL -> crash. The failure is INSIDE sub_001A0B0C, the lifted RtlAllocateHeap, which is SEH-wrapped. Also tested: applying the __SEH_epilog fix on top of the current build is much WORSE (200 -> 50 calls, heap 49 -> 2); reverted per Rule #14. That fix was tuned against the old esp drift and the fall-through fix changed it.",
    "commit": "053057a"
  },
  {
    "kernel_calls": 200,
    "failed_icalls": 6,
    "total_icalls": 339033,
    "heap_allocs": 49,
    "crash_in": "sub_00346743+0x130",
    "fault_va": "0xFFFFFF6C",
    "date": "2026-08-04",
    "message": "Fix the heap-handle clobber: _heap_init nulled a working handle on its 49th call",
    "note": "ROOT CAUSE, fully probed. _heap_init (sub_001A23F3) runs 49 times, each reserving a fresh 1 MB heap (00F81000, 01081000, ... exactly 1 MB apart). That exhausts a 64 MB console, so the 49th RtlCreateHeap fails - and the store is UNCONDITIONAL (store, then check), so it overwrote a WORKING handle with 0. Probes: '[HAIN] IN heap=00000000 size=6800' -> '[HAOUT] OUT ptr=00000000'. Every HeapAlloc(GetProcessHeap(),...) after that returned NULL, which left the default allocator NULL and crashed the class registrar. Guard keeps the existing handle when a later create fails. Result: crash MOVED (RVA 0xCF2590 -> 0x134E79, now sub_00346743+0x130), failed_icalls 7 -> 6, kernel_calls held at 200. Corrections logged earlier this session: 0x5D9C04 is _newmode not a heap handle, and the 0x5BB700 table is a symptom not a cause. NOT fixed: the 48 leaked heaps. Whatever re-enters CRT init 49 times is a separate bug and probably the next big win.",
    "commit": "053057a"
  },
  {
    "kernel_calls": 200,
    "failed_icalls": 6,
    "total_icalls": 339033,
    "heap_allocs": 49,
    "crash_in": "sub_00346743+0x130",
    "fault_va": "0xFFFFFF6C",
    "date": "2026-08-04",
    "message": "Find the 49x _heap_init: XAPI process startup re-enters itself 71 times",
    "note": "sub_001A23F3 has NO direct callers in generated code - it is called once from sub_001A1C23, the XAPI process-startup override in recomp_manual.c. Counted sub_001A1C23: 71 entries, with esp falling monotonically (00F7FF8C, 00F7FF0C, 00F7FE6C, 00F7FDAC, 00F7FCCC) - straight recursion, ~128 bytes per frame. Cause is the port's synchronous thread model: kernel_bridge runs PsCreateSystemThreadEx bodies inline, so a thread created during startup re-enters startup instead of running concurrently. Each pass re-ran _heap_init -> a fresh 1 MB heap -> 49 heaps -> exhaustion on a 64 MB console. TESTED AND REVERTED: a re-entrancy guard on sub_001A1C23 looks obviously correct and is badly wrong - those nested passes are how threads actually run here. 200 -> 44 kernel calls, heap 49 -> 2, failed icalls 6 -> 243. Left a comment at the call site so nobody re-tries it. The existing clobber guard already contains the damage (the good handle survives the failing 49th create), so 200 calls / 339k icalls holds. The real fix is the thread model, not a guard.",
    "commit": "17b4896"
  },
  {
    "kernel_calls": 200,
    "failed_icalls": 234,
    "total_icalls": 47621890,
    "heap_allocs": 49,
    "crash_in": null,
    "fault_va": null,
    "date": "2026-08-04",
    "message": "Fix TLS: XBE TLS directory was never processed - 339K -> 47.6M indirect calls",
    "note": "Crash was sub_00346743+0x130 reading 0xFFFFFF6C. Two real bugs, both confirmed against ground truth. (1) fs:[4] is the TLS slot array on Xbox, NOT the NT_TIB StackBase. xbox_memory_layout.c set it to XBOX_STACK_TOP on the Win32 assumption; all NINE MEM32(4) sites in the lifted code are the same pattern - idx=MEM32(0x5BA794); block=MEM32(fs4 + idx*4); block[+8]/[+0xC] - and not one treats it as a stack address. Now points at a real 16-slot array. (2) __tls_index held -37, so the lookup indexed 148 bytes BELOW the array. The XBE's TLS directory names tls_index_addr = 0x005BA794, which matches the read address and Ghidra's XAPILIB___tls_index exactly - the port never processed that directory, so the loader-assigned slot was never written. Set it to 0 in the XAPI startup override. Result: crash GONE (2/2 clean), total indirect calls 339,033 -> 47,621,890 (140x more code executing). kernel_calls flat at 200 - the proxy is blind again. New failure mode: 234 failed icalls on sequential small integers (0x0,0x1,0x2,...), the index-table-as-function-pointer pattern, now surfacing at far greater depth. Context: the lifter drops fs: prefixes entirely - 2,660 sites, mostly MEM32(0) for fs:[0] SEH - and the fake TIB at VA 0 is the workaround.",
    "commit": "17b4896"
  },
  {
    "kernel_calls": 200,
    "failed_icalls": 234,
    "total_icalls": 47621890,
    "heap_allocs": 49,
    "crash_in": null,
    "fault_va": null,
    "date": "2026-08-04",
    "message": "CORRECTION: the 47.6M indirect calls is a watchdog spin, not execution depth",
    "note": "Checked how the run actually ends: '[WATCHDOG] No progress after 8000 ms - main thread likely hung', killed after 47,621,967 ICALL dispatches. The last 16 targets are garbage that decodes as x86 instruction bytes - 0x0C2444D9 is D9 44 24 0C = 'fld dword ptr [esp+0xC]' - so it is calling through pointers read out of the middle of the instruction stream, looping until the watchdog fires. The TLS fix is still correct and the crash is genuinely gone, but 339K -> 47.6M measures spinning, not work. total_icalls as a progress signal is GAMEABLE by exactly this and I over-claimed on it twice. Trustworthy signals remain kernel_calls (200) and heap_allocs (49), which are counts of completed operations; both are flat. Real state: still inside CRT/XAPI runtime init, nothing rendered, nowhere near the main-menu definition of done.",
    "commit": "ffb054f"
  },
  {
    "kernel_calls": 200,
    "failed_icalls": 234,
    "total_icalls": 47621967,
    "distinct_ordinals": 15,
    "ended": "hang",
    "heap_allocs": 49,
    "crash_in": null,
    "fault_va": null,
    "date": "2026-08-04",
    "message": "Make the progress metric spin-proof",
    "note": "total_icalls counts dispatches, so an infinite loop inflates it without bound - it reported 47.6M for a watchdog spin and I called that '140x more code executing' before checking how the run ended. Now: (1) 'ended' records hang/crash/ended, parsed from the WATCHDOG line, so a hang and a clean finish stop looking identical; (2) 'distinct_ordinals' counts DISTINCT kernel functions reached - spin-proof, because a loop hammers the same handful forever and only reaching a new subsystem moves it; (3) total_icalls no longer counts as improvement when the run ended in a spin, and prints a warning when it did.",
    "commit": "ffb054f"
  },
  {
    "kernel_calls": 200,
    "failed_icalls": 234,
    "total_icalls": 47621967,
    "distinct_ordinals": 15,
    "ended": "hang",
    "heap_allocs": 49,
    "crash_in": null,
    "fault_va": null,
    "date": "2026-08-04",
    "message": "Spin located: __cinit's constructor-table cursor is destroyed by sub_003556E0",
    "note": "Probed sub_001A1C23 step by step: steps 1-3 complete 381 times each; step 4 sub_001A3554 = XAPILIB___cinit enters 381 times and NEVER returns. It walks two ctor tables; table A (5 entries) is fine, table B (0x44A590..0x44B0A4, 709 entries) breaks. Probing the cursor after each ctor names the exact one: slot 0x0044A598 -> sub_003556E0, after which esi = 0x00F7FF0C, a STACK address instead of 0x0044A59C. esi is the callee-saved cursor, so the loop then walks the stack calling garbage - the ring buffer shows a clean cycle (0x20000000, 0xC71FEE10, then stack addresses climbing 0x20 per pass). Same class as the fall-through-edge bug: a callee not restoring a callee-saved register. sub_003556E0 itself never touches esi, and its immediate callees sub_000119E0 and sub_00340DE6 are both balanced, so the clobber is deeper - sub_00011800 is next. CORRECTION: I attributed the 381 nested startups to XapiBootToDash relaunching; the BootToDash stub was never hit (it is called directly, bypassing recomp_lookup_manual), so that was wrong - the recursion comes from __cinit. Stub kept as defence anyway.",
    "commit": "ffb054f"
  },
  {
    "kernel_calls": 200,
    "failed_icalls": 6,
    "total_icalls": 307753,
    "distinct_ordinals": 15,
    "ended": "crash",
    "heap_allocs": 49,
    "crash_in": "sub_001A01D3+0x8B1",
    "fault_va": "0x7D6A9005",
    "date": "2026-08-04",
    "message": "Sync g_seh_ebp before __SEH_epilog: spin gone, failed icalls 234 -> 6",
    "note": "Chain, each link probed not assumed: __cinit's ctor cursor (esi) is destroyed by ctor sub_003556E0. Bracketing its two calls showed sub_000119E0 clean (esi 0044A598 in and out) and sub_00340DE6 breaking it (esi -> 00F7FF0C, esp -68). sub_00340DE6 never touches esi; its callee sub_00340DAE is SEH-wrapped, and __SEH_epilog restores ebx/esi/edi by popping at a drifted esp. ROOT: __SEH_epilog reads its frame from the GLOBAL g_seh_ebp, and 0 of 58 call sites synced it first - the lifter emits 'g_seh_ebp = ebp;' before tail jumps but not before epilog calls, so a nested SEH function overwrites the global and the outer epilog restores from the WRONG frame. That also explains why the [ebp-24] save-area fix made things worse twice: it makes the epilog depend on ebp, which was stale. Synced all 58 sites. Result: hang GONE (now a definite crash), failed_icalls 234 -> 6, safe_stub 731 -> 12, kernel_calls held at 200. New crash sub_001A01D3+0x8B1 writing 0x7D6A9005, reached via sub_001A0B0C (RtlAllocateHeap) from __cinit. Corrections this session: sub_00011800 does NOT clobber esi (3 instructions, never touches it) and XapiBootToDash was NOT the recursion cause.",
    "commit": "f9343f9"
  },
  {
    "kernel_calls": 200,
    "failed_icalls": 6,
    "total_icalls": 307753,
    "distinct_ordinals": 15,
    "ended": "crash",
    "heap_allocs": 49,
    "crash_in": "sub_001A01D3+0x8B1",
    "fault_va": "0x7D6A9005",
    "date": "2026-08-04",
    "message": "Save-area fix retried a third time and rejected: it is superseded, not mis-sequenced",
    "note": "Applied the [ebp-24] __SEH_epilog fix again now that the frame is published: 200 -> 52 kernel calls, heap 49 -> 2. Third rejection (40, 50, 52 against a 200 baseline). Reverted. Verified the premise against __SEH_prolog rather than trusting the comment - it really does 'MEM32(ebp-24) = esp' immediately after pushing ebx, esi, edi, so [save+0]=edi, [save+4]=esi, [save+8]=ebx and [save-4]=return address are all CORRECT offsets. The fix is not wrong about the layout; it is solving a problem that no longer exists. It was a workaround for esp drift, and the fall-through-edge fix plus the g_seh_ebp sync removed the drift at source. With esp no longer drifting the original POP32s read the right slots, and the save-area read now INTRODUCES error rather than avoiding it. Conclusion: retire it. Keep the drift fixed at source instead of compensating downstream.",
    "commit": "548ba05"
  },
  {
    "kernel_calls": 44,
    "failed_icalls": 2,
    "total_icalls": 69,
    "distinct_ordinals": 15,
    "ended": "crash",
    "heap_allocs": 2,
    "crash_in": "sub_003432FC+0x151",
    "fault_va": "0xFFFFFFFC",
    "date": "2026-08-04",
    "message": "Heap-range guards tested and KEPT: still load-bearing, unlike the save-area fix",
    "note": "Hypothesis: since the __SEH_epilog save-area workaround became actively harmful once esp drift was fixed at source, the other drift-era guards might have too. Five edits in the store cite stack escape/drift as their reason; 14 heap-range 'this' guards exist in gen. Disabled all 14 (condition made unreachable, exactly reversible) and measured: 200 -> 44 kernel calls, heap 49 -> 2. Re-enabled. So the hypothesis is WRONG - these guards still earn their place, and 'workarounds outlive their causes' does not generalise from one case. The pointers they reject are still genuinely bad, which means there is remaining corruption they are absorbing; that is worth finding, but they must not be removed first. Also hit CLAUDE.md trap #1 on the way (built a C string literal through a shell heredoc, the \\n arrived as a real newline and broke the build) - should have used add_probe.py.",
    "commit": "732d58e"
  },
  {
    "kernel_calls": 200,
    "failed_icalls": 6,
    "total_icalls": 307753,
    "distinct_ordinals": 15,
    "ended": "crash",
    "heap_allocs": 49,
    "crash_in": "sub_001A01D3+0x8B1",
    "fault_va": "0x7D6A9005",
    "date": "2026-08-04",
    "message": "Restored: 200 kernel calls, guards re-enabled, tree clean",
    "note": null,
    "commit": "732d58e"
  },
  {
    "kernel_calls": 200,
    "failed_icalls": 6,
    "total_icalls": 307753,
    "distinct_ordinals": 15,
    "ended": "crash",
    "heap_allocs": 49,
    "crash_in": "sub_001A01D3+0x8B1",
    "fault_va": "0x7D6A9005",
    "date": "2026-08-04",
    "message": "Guard instrumentation: only 2 of 14 guards are live, both in SEH exception dispatch",
    "note": "Probed all 9 distinct guard sites with add_probe.py. SEVEN NEVER EXECUTE (sub_00123600, sub_001F75A3, sub_00202CC0, sub_0020E520 x2, sub_0020E547, sub_002155B0) - they sit on paths the boot no longer takes, so they are dead weight, not protection. Only two fire on the live path, and both are SEH internals: sub_003432FC is _except_handler3 (Ghidra signature (ExceptionRecord, EstablisherFrame, Context), tests *(param_1+4) & 6 for EXCEPTION_UNWINDING, and __SEH_prolog pushes 0x3432FC as exactly that handler), and sub_003433B1 is the unwinder it calls. Entered 3 times each. G9 fires with ebx=0 - a NULL establisher/context frame handed to the unwinder. So the thing 'corrupting pointers' is not random memory damage: SEH exception dispatch is running during __cinit and unwinding with a null frame. That also explains why disabling the guards cost 200 -> 44: the two live ones are absorbing a real null, not a phantom. Next: find what raises the exception - a faulting constructor, or normal scope-exit unwind the port mis-models.",
    "commit": "b07c1e7"
  },
  {
    "kernel_calls": 200,
    "failed_icalls": 6,
    "total_icalls": 307753,
    "distinct_ordinals": 15,
    "ended": "crash",
    "heap_allocs": 49,
    "crash_in": "sub_001A01D3+0x8B1",
    "fault_va": "0x7D6A9005",
    "date": "2026-08-04",
    "message": "SEH during __cinit is NORMAL scope-exit unwinding, not a fault",
    "note": "Traced it: no exception is being raised at all. RtlRaiseException called 0 times (the bridge logs every call), VEH caught 0 host faults, and RtlUnwind (ordinal 292) is not among the 15 ordinals used. _except_handler3 (sub_003432FC) has ZERO direct callers - it is only ever pushed as a handler address (6 sites) and entered indirectly. The caller is sub_0034139A, which Ghidra's FidDb identifies outright as __local_unwind2, 'Library Function - Single Match, Visual Studio 2003 Release'. That is MSVC try/finally SCOPE-EXIT unwinding: __cinit wraps constructors in try/finally so a throwing ctor unwinds cleanly, and __local_unwind2 runs on normal exit too. So SEH machinery running during __cinit is correct CRT behaviour, not evidence of a fault - I was hunting an exception that does not exist. Follow-on: the ebx=0 that sub_003433B1's guard rejects is very likely a LEGITIMATE null ContextRecord, which is normal during unwind (context is not needed). If so that guard skips work on correct input. It cannot simply be deleted though - disabling the guards cost 200 -> 44 - so the dependency needs isolating rather than assuming.",
    "commit": "2ba694a"
  },
  {
    "kernel_calls": 200,
    "failed_icalls": 6,
    "total_icalls": 307753,
    "distinct_ordinals": 15,
    "ended": "crash",
    "heap_allocs": 49,
    "crash_in": "sub_001A01D3+0x8B1",
    "fault_va": "0x7D6A9005",
    "date": "2026-08-04",
    "message": "Guard dependency isolated: 1 of 14 is load-bearing, and it masks a null EstablisherFrame",
    "note": "Disabling ONLY sub_003433B1's guard reproduces the whole regression - 200 -> 44 kernel calls, heap 49 -> 2, identical to disabling all fourteen. So exactly one guard is load-bearing and the other thirteen are inert (seven of those never execute at all). What it protects: 'if (!(ebx in heap range)) { eax = 1; return; }' sits immediately before 'ebp = ebx + 0x10', which derives a FRAME POINTER from ebx. record+0x10 is the classic _except_handler3 frame-base derivation, so ebx is the EstablisherFrame, not the Context as the guard comment says - and it is NULL. Without the guard ebp becomes 0x10 and everything downstream runs on a bogus frame; measured, that spins with ~9,000 safe-stub hits (and was the first NON-DETERMINISTIC result smoke_spread has flagged, 9079 vs 9088). CORRECTION to the previous entry: I guessed ebx=0 was a legitimate null ContextRecord and that the guard was skipping work on correct input. Wrong - an EstablisherFrame is never legitimately null. The guard is correct and necessary; it is masking a real defect upstream. Next: _except_handler3 is entered with a null EstablisherFrame, and that value comes off the SEH chain at fs:[0] - which the port fakes at VA 0 as a single global head, with no per-thread separation.",
    "commit": "87d0896"
  },
  {
    "kernel_calls": 200,
    "failed_icalls": 6,
    "total_icalls": 307753,
    "distinct_ordinals": 15,
    "ended": "crash",
    "heap_allocs": 49,
    "crash_in": "sub_001A01D3+0x8B1",
    "fault_va": "0x7D6A9005",
    "date": "2026-08-04",
    "message": "Null EstablisherFrame traced to a corrupt SEH chain head - NOT fixed, needs a design change",
    "note": "Probed _except_handler3's actual arguments. Two patterns alternate: (a) ExcRec=0 EstFrame=0 Ctx=001A1C23 Disp=0 - Ctx holding a FUNCTION ADDRESS (sub_001A1C23, the XAPI startup) means these are not SEH arguments at all, just stack garbage; (b) ExcRec=FFFFFFFF EstFrame=00F7FF98 - the end-of-chain marker arriving as an ExceptionRecord. In every case chainhead (fs:[0], i.e. MEM32(0)) reads 0x000000FF. xbox_memory_layout.c initialises it to 0xFFFFFFFF, so a DWORD write put 0xFF there - and the only writer on that path is __SEH_epilog's 'MEM32(0) = ecx' with ecx = MEM32(ebp-16), i.e. a corrupt frame. That is circular: corrupt frame -> corrupt chain -> corrupt frame. Ruled out: the single MEM8(0) site is in sub_00067DF0 which is never reached, and a byte write of 0xFF over 0xFFFFFFFF would leave 0xFFFFFFFF anyway. DELIBERATELY NOT PATCHED. The guard at sub_003433B1 already contains the damage and is worth 156 kernel calls; a speculative fix on top of a self-reinforcing corruption would most likely just move it. The real issue is structural: 63 SEH prologs and 58 epilogs all share ONE chain head, because the lifter drops fs: prefixes (2,660 sites) and the port fakes a single global TIB at VA 0. Correct fix is per-context SEH state, not another guard.",
    "commit": "97262db"
  },
  {
    "kernel_calls": 200,
    "failed_icalls": 6,
    "total_icalls": 307753,
    "distinct_ordinals": 15,
    "ended": "crash",
    "heap_allocs": 49,
    "crash_in": "sub_001A01D3+0x8B1",
    "fault_va": "0x7D6A9005",
    "date": "2026-08-04",
    "message": "SEH chain head is ALREADY corrupt before any SEH runs - narrows the scope",
    "note": "Probed the very first __SEH_prolog: it reads chainhead = 0x000000FF, not the 0xFFFFFFFF that xbox_memory_layout.c writes (the TIB init log confirms it ran). And at the first __SEH_epilog, ebp is a perfectly valid stack address (00F7FF2C) with [ebp-16] already holding 0xFF. So the corruption is NOT produced by the unwinding machinery - VA 0 is already wrong before the first SEH prolog executes. That invalidates Option A in docs/SEH_SCOPE.md, which assumed the epilog wrote bad values from a bad frame; the epilog is faithfully saving and restoring a value that was already corrupt. Also corrects an earlier claim: I said the single MEM8(0) site (sub_00067DF0) was never reached, based on grepping stderr for the function name - but function names are not logged, so that check was meaningless. Probed it properly: 0 hits, it genuinely never runs. Remaining question is narrow and well-posed: what changes VA 0 from 0xFFFFFFFF to 0x000000FF between xbox_memory_layout init and the first SEH prolog? 0x000000FF is 0xFFFFFFFF with the top three bytes zeroed, or a dword write of 0xFF.",
    "commit": "9373bf6"
  },
  {
    "kernel_calls": 82,
    "failed_icalls": 4,
    "total_icalls": 79,
    "distinct_ordinals": 15,
    "ended": "crash",
    "heap_allocs": 3,
    "crash_in": "sub_0034139A+0x1BA",
    "fault_va": "0xF42BFFE0",
    "date": "2026-08-04",
    "message": "ROOT CAUSE: ordinal 47 was wired to the wrong handler and wrote through a BOOLEAN",
    "note": "Built a TIB write-watch in main.c (VirtualProtect the VA-0 page read-only after init; the VEH names the first writer, then self-disarms). It caught the corruption exactly: chain head was 0xFFFFFFFF at arm time, and the first write was to Xbox VA 0x00000001 from the kernel bridge - a zero dword at VA 1 turns bytes FF FF FF FF into FF 00 00 00, which reads as 0x000000FF. Exact match for the value seen. Cause: the bridge handler table mapped ordinal 47 to bridge_HalReadSMCTrayState, which treats arg1 as an OUTPUT POINTER. Ordinal 47 is actually HalRegisterShutdownNotification(Registration, Register) - kernel_thunks.c had the right name all along, only the bridge table disagreed - and its arg1 is the BOOLEAN 1. So 'if (count_ptr) BRIDGE_MEM32(count_ptr) = 0;' wrote through the address 1. The game's own call site (push ebx=1; push 0x461694) was already documented in a comment two hundred lines away. Fixed: added bridge_HalRegisterShutdownNotification (writes nothing), moved HalReadSMCTrayState to its real ordinal 46 (previously unmapped), and added BRIDGE_PTR_OK - 'if (ptr)' only rejects 0, so flags passed as pointers sailed through it. Verified: first TIB write is now sub_003432A8 (__SEH_prolog) at VA 0, legitimately installing the chain, and zero corrupt-chainhead reports. Kernel calls 200 -> 82 - Rule #1's two-different-programs case, the boot now takes a correct path and stops elsewhere: sub_0034139A+0x1BA (__local_unwind2) reading 0xF42BFFE0.",
    "commit": "2f9134c"
  },
  {
    "kernel_calls": 82,
    "failed_icalls": 4,
    "total_icalls": 79,
    "distinct_ordinals": 15,
    "ended": "crash",
    "heap_allocs": 3,
    "crash_in": "sub_0034139A+0x25A",
    "fault_va": "0xF42BFFE0",
    "date": "2026-08-04",
    "message": "Mechanism at 82 calls fully mapped: _except_handler3 gets a non-record as its EstablisherFrame",
    "note": "Probed the very first __local_unwind2 call: rec=00F7FF98 handler=00000000 scopetab=00F7FFD4 trylevel=00000000. A real SEH registration record is {prev, handler, scopetable, trylevel} - handler is never 0 and scopetable is a .data pointer like 0x430838, not a stack address. So the EstablisherFrame handed to _except_handler3 is not a registration record, just arbitrary stack memory. __local_unwind2 then does exactly what it is written to do: reads trylevel 0, passes the !=-1 and !=param_2 checks, reads scopetable[0].previousTryLevel from stack garbage, WRITES THAT BACK into the record's trylevel field, calls scopetable[0].handler (also garbage), and on the next iteration reads the corrupted trylevel 0x3CCD0003, multiplies by 3 and faults. It destroys its own loop variable on the first pass. Also self-feeding: those bogus handler calls are mostly rejected by the plausibility filter as stack addresses, but one lands on 0x3432FC and re-enters _except_handler3. Confirmed via the icall ring buffer at handler entry: targets were 003432FC, 00F7FF10, 00F7FF0C - consecutive stack slots. sub_003433B1 is a FRAGMENT of _except_handler3 (181 bytes in), and its guard admits [0x880000,0x4000000), a range the stack falls inside, so it cannot reject a stack address that is not a record. Remaining question: what invokes _except_handler3 with a non-record, given nothing raises an exception (0 RtlRaiseException, 0 VEH, no RtlUnwind) - the call arrives from inside __cinit.",
    "commit": "c52fe7e"
  },
  {
    "kernel_calls": 82,
    "failed_icalls": 4,
    "total_icalls": 79,
    "distinct_ordinals": 15,
    "ended": "crash",
    "heap_allocs": 3,
    "crash_in": "sub_0034139A+0x1BA",
    "fault_va": "0xF42BFFE0",
    "date": "2026-08-04",
    "message": "Global x87 stack: the FPU is a machine resource, not per-frame",
    "note": "The lifter emitted 'double _fp_stack[8]; int _fp_top = 0;' as FUNCTION LOCALS. On x86 the x87 stack is global state - a function returning float or double leaves the value in st(0) and the caller pops it with fstp. With a per-frame stack every such return value was destroyed at the call boundary and the caller popped an uninitialised slot. Exactly the ebp/g_seh_ebp bug class. Fixed in translator.py (macros now alias g_fp_stack/g_fp_top, defined in xbox_memory_layout.c) and applied to the existing gen/ by tools_data/apply_global_fpstack.py, which emits byte-identical lines so the pending regeneration is a no-op for them. Also added fp_st(n) for the register forms Stage B needs. Signals unchanged at 82/4/3/8 with an identical crash site over 2 runs - expected, since the boot dies in __local_unwind2 long before float returns matter. Recorded as a correctness fix with no boot signal, not as progress.",
    "commit": "a16e510"
  },
  {
    "kernel_calls": 82,
    "failed_icalls": 4,
    "total_icalls": 79,
    "distinct_ordinals": 15,
    "ended": "crash",
    "heap_allocs": 3,
    "crash_in": "sub_0034139A+0x1BA",
    "fault_va": "0xF42BFFE0",
    "date": "2026-08-04",
    "message": "x87 and MMX implemented: 9,099 dropped instructions -> 182, plus 16,147 silent stack-depth fixes",
    "note": "Measured the real gap first instead of trusting the earlier '550 missing instructions' estimate, which was counted wrong twice - once too low (my grep only saw the '/* mnemonic' form and missed the 623 MMX sites emitted as '/* TODO:'), once too high in kind (the biggest hole was never SIMD at all, it was x87). THE DROPPED SET: fnstsw 3556, fstp 1927, fld 1435, fcos 448, movq 272, fdivr 236, fsubr 201, paddw 152, psraw 88, packuswb 87, pextrw 84, fsin 60, psrlq 37, paddusw 32, punpcklbw 30, pmullw 24 and a long tail - 9,099 instructions that emitted a bare C comment. Now 182, and 172 of those are correct: 90 are 3DNow! (pfmul/pfadd), unreachable because recomp_cpuid reports an Intel Pentium III and the game dispatches on AMD extended bits; 18 emms/femms, which are no-ops here because MMX is modelled independently of x87 rather than aliased onto it; 6 prefetch hints; 2 fnclex. The genuine remainder is 62 packed-float sites (shufps 56, unpcklps/unpckhps/movhlps 2 each) - see below. THE SILENT-WRONG SET, which no drop count showed and no build error could catch: the x87 model had the stack DEPTH wrong in four places, and depth errors cascade because every st(n) is relative to TOP. fst popped when it must not (627 sites), fcomp/fcompp/fucompp never popped when they must (3,146), and fadd/fsub/fmul/fdiv popped unconditionally even for a memory operand, which never pops (12,374). 16,147 sites total, now verified correct: every non-p arithmetic form emits no pop and every p-form does. ARCHITECTURAL: _fp_stack/_fp_top were FUNCTION LOCALS. The x87 stack is a machine resource - a function returning float or double leaves it in st(0) for the caller to pop - so every float return value was destroyed at the call boundary. Same bug class as ebp, same fix as g_seh_ebp: they are now g_fp_stack/g_fp_top. VERIFICATION: tools/recomp/test_fpu.py checks mnemonic decomposition and per-form stack depth (37 cases); tools_data/test_mmx_helpers.c compiles standalone and checks 30 lane-order, saturation and packing boundaries - it caught one bad expectation of mine, not a bad helper. Signals unchanged at 82/4/3/8, deterministic over 2 runs, identical crash site: the boot still dies in __local_unwind2 long before this code runs, so this is recorded as correctness with no boot signal, NOT as progress. NEXT: xmm is modelled as a single C float, but it is 128 bits - four lanes. That is why shufps and the packed-float ops still emit comments, and it means movaps only copies a quarter of the register. Contained job, same class as this one.",
    "commit": "a16e510"
  },
  {
    "kernel_calls": 52,
    "failed_icalls": 1,
    "total_icalls": 13,
    "distinct_ordinals": 15,
    "ended": "crash",
    "heap_allocs": 2,
    "crash_in": "sub_002235D0+0xC0",
    "fault_va": "0xFFFFFF44",
    "date": "2026-08-04",
    "message": "ROOT CAUSE: an unresolved stub never purged its args, destroying _initterm's table cursor",
    "note": "The boot died in __local_unwind2 with an EstablisherFrame that was not a registration record, in a run where nothing ever raised an exception. Chain, measured not inferred: _initterm (sub_001A3554) walks the CRT initialiser table with the cursor in esi; after calling entry 0x44A598 the cursor came back as 0x00F7FF0C, a STACK address, so the loop left the table and called every dword it found - one of which was _except_handler3, which is how the SEH unwinder was entered with garbage. Traced down: _initterm -> sub_003556E0 -> sub_00340DE6 -> sub_00340DAE -> sub_00340D06 -> sub_00345ACC, where esp was measured drifting 0x10 across the call to sub_001A016A, so __SEH_epilog restored ebx/esi/edi from 16 bytes off-target. The drift came from sub_001A0189, which was an EMPTY STUB in recomp_stubs_unresolved.c ('not detected'). The real code is 5 instructions ending in 'ret 0xc'. In this calling convention the callee pops the fake return address, so a do-nothing stub pops NOTHING and leaks 16 bytes of simulated stack per call. Fixed by seeding it (seed_missing_functions.py --va 0x001A0189). THIS IS A CLASS, NOT A ONE-OFF: there are 3,205 unresolved stubs and every one of them is a silent stack leak whenever it is called. RESULT: kernel calls 82 -> 52, but that count is the wrong lens (Rule #8) - failed indirect calls 4 -> 1 and safe-stub hits 8 -> 1, because the stack-walking garbage loop is gone entirely. The boot now reaches GAME code (sub_002235D0/sub_002221E0/sub_00209650 at 0x22xxxx) instead of dying in the CRT at 0x34xxxx. New blocker is the same class: esp has gone NEGATIVE (0xFFFFFF44). TOOLS BUILT: recomp_where() + add_probe --where + triage --where (probe-callable native backtrace); RECOMP_CHECK_ABI (verifies ebx/esi/edi across every direct call - found this chain in ONE run); find_reg_clobbers.py; dump_table.py. ALSO FIXED A MEASUREMENT BUG: triage_crash.py's load_map() stopped at 'entry point at' and never read MSVC's Static symbols section, loading 30,582 of 55,789 symbols - so every backtrace silently attributed addresses to the nearest LOADED symbol with a plausible-looking offset. It reported sub_001A016A+0x1CB for a function 0x60 bytes long. Every backtrace-based conclusion before this fix is suspect.",
    "commit": "a16e510"
  },
  {
    "kernel_calls": 56,
    "failed_icalls": 3,
    "total_icalls": 96,
    "distinct_ordinals": 15,
    "ended": "crash",
    "heap_allocs": 2,
    "crash_in": "sub_002235D0+0xC0",
    "fault_va": "0xFFFFFF80",
    "date": "2026-08-04",
    "message": "Seeded 1,049 unresolved stubs - the empty bodies were swallowing register restores and stack purges",
    "note": "Follow-on from the sub_001A0189 root cause. recomp_stubs_unresolved.c defined 3,205 addresses as EMPTY bodies, and all 3,204 that remain are actually called. In this calling convention the call site pushes a fake return address that the callee must pop, so an empty stub pops nothing and leaks 4 bytes of simulated stack per call, plus whatever argument purge the real function owed. 928 of them are reached by a lifter TAIL call, which is worse: they terminate a fragment chain, so the PUSH32/POP32 pairs that restore ebx/esi/edi across the chain never complete and the caller silently continues with someone else's registers. Confirmed on sub_001183A0 -> sub_001183C0 -> sub_001183ED, where the stub held the actual 'pop edi; pop esi'. Seeded 1,049 (908 tail-called + 121 new call targets the seeded bodies introduced + closure rounds); 9 have no findable terminating ret and now at least pop the return address. RESULT 52 -> 56 kernel calls, but failed icalls 1 -> 3 and safe-stub 1 -> 5. NOT treating the rises as a regression: 1,049 functions that previously did nothing now execute real code and make real indirect calls, so more of them can fail - the same 'two different programs' reading as the 200 -> 82 step. Verified the ABI checker is behaviour-neutral by rebuilding with it OFF: identical 56/3/2/5. BLOCKER UNCHANGED: esp goes NEGATIVE (0xFFFFFF80) in sub_002235D0, reached via sub_002221E0 <- sub_00209650 <- sub_002226E0. RECOMP_CHECK_ABI's esp guard reported nothing, so esp is not escaping across a direct call - it is going wrong inside a function or across an indirect call, which RECOMP_ABI_CALL does not wrap. That is the next thing to instrument. TOOL FIXES: seed_missing_functions.py overwrote recomp_seed.c each run, silently discarding the previous round - and because the addresses were already recorded as known starts, re-running did NOT restore them (908 functions became 121). It now carries forward existing bodies. stub_overridden.py could not see the one-line stub form and treated recomp_seed.c as a target rather than a source. Added normalise_seed_names.py: the seeder emits XbSymbolDatabase names for call targets while the main generation emits sub_ADDRESS, so 21 references failed to link.",
    "commit": "a16e510"
  },
  {
    "kernel_calls": 56,
    "failed_icalls": 3,
    "total_icalls": 96,
    "distinct_ordinals": 15,
    "ended": "crash",
    "heap_allocs": 2,
    "crash_in": "sub_002235D0+0xC0",
    "fault_va": "0xFFFFFF80",
    "date": "2026-08-04",
    "message": "Blocker fully characterised: infinite recursion in Alchemy's igMetaObject bootstrap",
    "note": "The negative esp is not a leak - it is unbounded recursion. Added an esp guard inside PUSH32 itself under RECOMP_CHECK_ABI (RECOMP_ABI_CALL only sees call boundaries, and this escapes inside a function), which named the exact push and showed a repeating 4-function cycle: sub_002235D0 -> sub_002226E0 -> sub_00209650 -> sub_002221E0 -> sub_002235D0. Each lap allocates 0x108 bytes, so esp walks the full 8 MB stack in roughly 33,000 laps and then leaves it. This is Intrinsic Alchemy, the engine the game is built on (see game/alchemy.ini): the strings involved are 'igMetaObject' at 0x3F9734 and 'igMetaObject: ' at 0x3F9860, so this is metaclass registration, and registering the ROOT metaclass is a bootstrap that has to break its own cycle. Ghidra gives the breaker exactly: FUN_002226e0 is 'if (*DAT_005bc508 != 0) { FUN_00209650(&LAB_002221e0); ... }' - the recursive registration path runs only when that byte is NON-zero; when it is zero the function takes FUN_001f7e00 and allocates directly, no recursion. So the guard is inverted in effect for us. Measured: MEM32(0x5BC508) = 0x00F81288, a heap object whose first dword is the vtable pointer 0x003F4770, so the byte read is 0x70 and the recursive path is always taken. 0x005BC508 lives in BSS and is 0x00000000 at load - confirmed with a write-watch, which armed on the value 0. So something sets it to a live object before igMetaObject itself is registered, and the bootstrap then eats itself. Not yet found WHAT sets it: the page-granular watch fired first on a neighbouring write to 0x005BC548 from sub_00011E40+0x1A1. Next step is a byte-exact watch on 0x5BC508, or working out which of the 357 callers of sub_002226E0 legitimately runs before the metaobject system is up. Signals unchanged at 56/3/2/5, deterministic, same crash site - this entry is analysis, not a fix.",
    "commit": "a16e510"
  },
  {
    "kernel_calls": 56,
    "failed_icalls": 3,
    "total_icalls": 96,
    "distinct_ordinals": 15,
    "ended": "crash",
    "heap_allocs": 2,
    "crash_in": "sub_002235D0+0xC0",
    "fault_va": "0xFFFFFF80",
    "date": "2026-08-04",
    "message": "Recursion traced to our own heap: xbox_HeapAlloc overwrites the core engine buffer",
    "note": "Made the write-watch byte-exact first. It was disarming on the first write anywhere in the 4 KB page, so a neighbour answered a question that was never asked - that is why the earlier attempt reported 0x5BC548 instead of 0x5BC508. It now steps the neighbouring write over with the trap flag, re-protects on the single-step, and keeps waiting; it also prints a backtrace and how many writes it stepped past. It needed 648 of them to reach the right one. CHAIN, ALL MEASURED: FUN_002226e0 takes the recursive igMetaObject registration path when the byte at *DAT_005bc508 is non-zero, and the direct-allocation path when it is zero. DAT_005bc508 is written exactly once, by 0x00239E50 - Ghidra names it alloc_core_engine_buffer - during a C++ static constructor under __cinit. That write is legitimate. The pointer lands on heap block 0x00F81288, and the watch confirmed that block was 0x00000000 when armed, which is the value the guard needs. The first write to it then came from xbox_HeapAlloc+0x94 (via bridge_NtAllocateVirtualMemory), NOT from game code - it wrote 0x003F4770, a .rdata vtable pointer. So OUR heap wrote into a block the game already owns: either it handed the same memory out twice, or its bookkeeping lives inside the region it returns. The game then reads a non-zero byte, believes the metaobject system is up before igMetaObject itself is registered, and the bootstrap eats itself - 33,000 laps of sub_002235D0 -> sub_002226E0 -> sub_00209650 -> sub_002221E0 until esp leaves the 8 MB stack. This is a bug in src/kernel, not in the lifted code, which is a much better place for it to be. Signals unchanged at 56/3/2/5 - analysis, not a fix.",
    "commit": "a16e510"
  },
  {
    "kernel_calls": 56,
    "failed_icalls": 3,
    "total_icalls": 96,
    "distinct_ordinals": 15,
    "ended": "crash",
    "heap_allocs": 2,
    "crash_in": "sub_002235D0+0xC0",
    "fault_va": "0xFFFFFF80",
    "date": "2026-08-04",
    "message": "Recursion narrowed to static-initializer ORDER, not a lifter bug",
    "note": "CORRECTION FIRST: I briefly concluded the lifter had dropped the 'already registered' guard at 0x00223614. It has not. recomp_0016.c:21403 carries it faithfully - 'if (CMP_NE(MEM32(ebx), 0)) goto loc_00223698' - and my grep pattern was simply wrong. Verified against Ghidra instruction by instruction over 0x0022360D..0x00223620. No lifter defect here. WHAT THE CYCLE ACTUALLY IS: FUN_002235d0 caches its result - the guard reads *param_1 (param_1 = 0x5BC274) and skips the whole creation block when non-zero - but the store '*param_1 = FUN_002226e0(...)' happens AFTER that call returns. So on the first entry the slot is legitimately 0, it calls FUN_002226e0, and if that re-enters registration the guard cannot help: nothing has been stored yet. Hardware must therefore never re-enter, and FUN_002226e0 is where it is decided: '*DAT_005bc508 != 0' takes the registration path, zero takes plain allocation via FUN_001f7e00. So on hardware *DAT_005bc508 is ZERO at this moment. MEASURED IN OUR PORT: DAT_005bc508 = 0x00F81288 with first byte 0x70 - the low byte of vtable pointer 0x003F4770 - so a constructed object already sits there. DAT_005bc508 is written exactly once, by 0x00239E50 (Ghidra: alloc_core_engine_buffer), from a C++ static constructor under __cinit. Ruled out with a byte-exact watch: the heap block was 0x00000000 when armed, and both early writes to it were xbox_HeapAlloc's own memset zeroing a fresh block - correct behaviour, not corruption. So the earlier 'our heap overwrote the buffer' reading was WRONG and is retracted. LEADING HYPOTHESIS NOW: static-initializer ORDER. alloc_core_engine_buffer runs before the igMetaObject bootstrap in our port; on hardware it must run after. _initterm (sub_001A3554) walks three sources in this order - MEM32(0x47A044), then [0x44B0A8,0x44B0BC), then [0x44A590,0x44B0A4) - and that order, or the boundary between the tables, is the thing to check next. TOOL: the write-watch is now byte-exact. It used to disarm on the first write anywhere in the 4 KB page, which is how a neighbour at 0x5BC548 answered a question about 0x5BC508. It now steps neighbours over with the trap flag and re-protects on the single-step; it took 648 of those to reach the right write. Added an optional 4th field, VA:SIZE:LABEL:SKIP, to let N matching writes past - the first write to a fresh heap block is always the allocator zeroing it. Signals unchanged at 56/3/2/5 - analysis only.",
    "commit": "a16e510"
  },
  {
    "kernel_calls": 56,
    "failed_icalls": 3,
    "total_icalls": 96,
    "distinct_ordinals": 15,
    "ended": "crash",
    "heap_allocs": 2,
    "crash_in": "sub_002235D0+0xC0",
    "fault_va": "0xFFFFFF80",
    "date": "2026-08-04",
    "message": "ROOT CAUSE of the igMetaObject recursion: threads run inline, so startup re-enters itself",
    "note": "Chased the guard byte to the end and it is NOT the bug - every piece of it is legitimate. alloc_core_engine_buffer (0x00239E50) allocates a block, constructs an object in it via sub_002154F0 (byte-exact watch, skipping the allocator's own memset, named the vtable write at sub_002154F0+0xAA), and stores the pointer in DAT_005bc508. The object's first byte is then the low byte of vtable 0x003F4770, i.e. 0x70, and FUN_002226e0 reads exactly that byte to decide between registering a metaclass and allocating directly. Also verified the 'already registered' guard in FUN_002235d0 is lifted faithfully AND reads the correct stack slot: probed it, ebx = 0x005BC274 (the igMetaObject slot) with the expected 0x120 displacement, and the slot is 0 every pass because the store only happens after the recursive call returns. THE ACTUAL CAUSE IS ORDERING. alloc_core_engine_buffer runs BEFORE the metaclass bootstrap, so the bootstrap starts in a world that already looks initialised and eats itself. Both paths arrive through bridge_PsCreateSystemThreadEx -> sub_0019F196 -> sub_001A1C23, at DIFFERENT offsets in the same function (+0x245 and +0x16B), which looked like out-of-order execution until sub_001A1C23 turned out to be a hand-written override in recomp_manual.c whose own comment states it: the port runs PsCreateSystemThreadEx start routines SYNCHRONOUSLY (kernel_bridge.c ~line 241, 'Call the start routine synchronously through the recomp dispatch'), so a thread created during startup runs to completion INSIDE startup - 71 nested entries measured. Those two offsets are different nested invocations, not a reordering. So the port has no concurrency: every worker thread the engine spawns during init runs immediately and completely, in the middle of the initialisation it depends on. The igMetaObject recursion is a symptom. NEXT TARGET IS THE THREAD MODEL, not this guard. The existing comment's warning still stands - do NOT block re-entry into sub_001A1C23, that was tried and cost 200 -> 44 kernel calls - because with inline threads the re-entry IS how threads run. The fix is to stop running them inline, which removes the need for re-entry rather than blocking it. Signals unchanged at 56/3/2/5.",
    "commit": "a16e510"
  },
  {
    "kernel_calls": 54,
    "failed_icalls": 4,
    "total_icalls": 60275,
    "distinct_ordinals": 15,
    "ended": "crash",
    "heap_allocs": 2,
    "crash_in": "sub_002235D0+0xEA",
    "fault_va": "0xFFFFFF88",
    "date": "2026-08-05",
    "message": "Recovered the tree after a botched stage-2 attempt; found gen/ is not reproducible from a commit",
    "note": "WHAT I BROKE: attempting per-thread SEH, I taught disasm.py to keep the fs: segment override and _fmt_mem to emit g_fs_base + X. It produced ZERO sites - Capstone's mem.segment is not populated the way I assumed - so the change achieved nothing. Worse, before regenerating I ran 'manual_edits.py extract', which swallowed the 1,049 seeded function bodies into the hand-edit store (139 entries -> 1042). The regeneration then could not place them and the tree stopped linking; a repair loop diverged (16 -> 18 -> 18 -> 14 unresolved). Backed up the broken gen/ and rebuilt from the commit. RECOVERY PATH THAT WORKED: git checkout of tracked sources to bee574d; restore the PRE-seed seeded_functions.json from 65de1fd^ (the seeder filters out addresses already recorded as known starts, so re-seeding against the post-seed file is a no-op - that is the trap); delete recomp_seed.c; full regeneration; the documented recipe; then seed_missing_functions.py --stubs --tail-only --apply plus closure rounds on whatever the linker still wants; one address (sub_0023BF80) has no terminating ret and takes a purging stub. FINDING WORTH MORE THAN THE FIX: the rebuilt tree measures 54/4/2/8 where the commit measured 56/3/2/5. Same sources, same recipe, different result - so gen/ is NOT deterministically reproducible from a commit. gen/ is gitignored, so a fresh clone cannot reproduce a recorded number, and every progress entry is only as trustworthy as the gen/ that produced it. The seeding is order-dependent: which stubs get seeded versus stubbed depends on what the linker happens to demand, and that depends on the previous round. Making this deterministic - seed the full tail-called set in one pass from a recorded list rather than iterating on linker errors - is now a prerequisite for trusting any A/B measurement. TWO PROCESS RULES EARNED: (1) never run 'manual_edits.py extract' after seeding, it captures seeded bodies as manual edits; (2) back up gen/ before ANY regeneration - CLAUDE.md already says so and I skipped it exactly once, which is why recovery cost a rebuild instead of an untar. Tracked drift left uncommitted: seeded_functions.json and recomp_manual.c differ from bee574d because the rebuild re-derived them.",
    "commit": "bee574d"
  },
  {
    "kernel_calls": 54,
    "failed_icalls": 4,
    "total_icalls": 60275,
    "distinct_ordinals": 15,
    "ended": "crash",
    "heap_allocs": 2,
    "crash_in": "sub_002235D0+0xEA",
    "fault_va": "0xFFFFFF88",
    "date": "2026-08-05",
    "message": "Regeneration itself is now the blocker - two attempts, both regressed, both recovered by snapshot",
    "note": "Set out to land the fs: change and got stopped by the pipeline, not the change. ATTEMPT 1: regenerate + recipe + closure -> 3 kernel calls. ATTEMPT 2: added --force to seed_missing_functions.py so recorded addresses can be re-seeded (the 'starts' filter makes re-seeding a silent no-op after the first run, which is what stalled the closure at 14) -> closure improved 14 -> 8, but the build measured 32/13/3/8 against a 54/4/2/8 baseline: kernel calls down a third, failed indirect calls tripled. Both times snapshot.py restored the baseline exactly, second time in a single step. THE PIPELINE IS THE PROBLEM. A regeneration cannot currently reproduce the tree it replaces. Seeding is order-dependent - which addresses get a real body versus a purging stub depends on what the linker demands next, which depends on the previous round - and 8 addresses (sub_001A237D, sub_0023BF80, sub_0035B270, sub_00361B60, sub_003620A3, sub_00368DF4, sub_00397554, sub_00397555) cannot be lifted at all because function_extent finds no terminating ret, so they become stubs however many rounds you run. sub_001A237D is XapiBootToDash, which already has a hand-written override, so stubbing it is actively wrong. THE fs: CHANGE IS NOT IMPLICATED: g_fs_base is 0, so MEM32(g_fs_base + X) is bit-identical to MEM32(X), and it is committed and verified (2,651 sites, checked on __SEH_prolog directly). It simply cannot reach a build until regeneration is trustworthy. WHAT THIS MEANS FOR THE ROADMAP: every remaining step - per-thread TIBs, real threads, the startup ordering fix that gets us to a menu - needs at least one regeneration. So regeneration reliability is now ahead of threads in priority, not a detour from it. Concretely: function_extent needs to handle the 8 no-ret cases (tail-jump terminated functions, and entries that are really mid-function labels), and seeding needs to run once from a recorded list rather than iterating on linker errors. Baseline restored and verified at 54/4/2/8, deterministic.",
    "commit": "c58666c"
  },
  {
    "kernel_calls": 54,
    "failed_icalls": 4,
    "total_icalls": 60275,
    "distinct_ordinals": 15,
    "ended": "crash",
    "heap_allocs": 2,
    "crash_in": "sub_002235D0+0xEA",
    "fault_va": "0xFFFFFF88",
    "date": "2026-08-05",
    "message": "The 195 skipped addresses are NOT junk - retracting that",
    "note": "Regenerated with the cleaned seed list. 864 seeded instead of 1,059, exactly the 195 the new classifier rejects as mid-function or override. Result: the closure went from 8 unresolved to 80, and the build still would not link. Restored the baseline; 54/4/2/8 confirmed. CORRECTION TO THE PREVIOUS ENTRY: I called those 195 addresses 'junk' and '18% of the seed set that was never valid'. That was wrong. They are mid-function call targets - real code that something genuinely calls. The classifier is right that the SEEDER should not manufacture them (a seeded body starting mid-instruction-stream and ending at whatever ret it reaches is a fabrication), but refusing to seed them does not make the calls go away. They fall through to purging stubs instead, which is the original defect this whole thread started from: an empty stub swallows the PUSH32/POP32 pairs that restore ebx/esi/edi. So a fabricated body was apparently serving better than no body. WHERE THE FIX ACTUALLY BELONGS: the lifter. It already splits functions at branch targets into fragments; these 195 are targets where the split did not happen, so nothing defines them. The right change is to make the splitter cover them - then they are real fragments of their containing function, with correct entry state, and neither the seeder nor a stub is involved. Concretely, sub_0023BF80 should be emitted as a label inside sub_0023BEE0's translation and reachable as a fragment, not conjured as a standalone function. WHAT IS SOLID FROM THIS ROUND: seeding replay is deterministic and proven (two runs byte-identical); --from-list is idempotent; the classifier correctly identifies what the seeder must not fabricate, and its output is now the worklist for the lifter fix. Snapshot restore used twice more today, both clean. Baseline unchanged at 54/4/2/8.",
    "commit": "0f6f8e7"
  },
  {
    "kernel_calls": 54,
    "failed_icalls": 4,
    "total_icalls": 60275,
    "distinct_ordinals": 15,
    "ended": "crash",
    "heap_allocs": 2,
    "crash_in": "sub_002235D0+0xEA",
    "fault_va": "0xFFFFFF88",
    "date": "2026-08-05",
    "message": "Every regeneration lands on 32/13/3/8 regardless of what changed - the recipe does not reproduce the baseline",
    "note": "Applied the fragment promotion (3,272 -> 3,009 stubs, 263 mid-function targets now real code) through the full documented recipe. Result: 32/13/3/8 against a 54/4/2/8 baseline. Restored, baseline re-confirmed. THE DECISIVE OBSERVATION: this is the SAME 32/13/3/8 the fs: regeneration produced earlier today, with a completely different change applied. Two unrelated changes, identical outcome. So the regression is not caused by either change - it is caused by regenerating at all. The documented recipe reproduces a tree that is roughly 22 kernel calls worse than the one we have, every time, deterministically. WHAT THAT MEANS: the working gen/ contains accumulated state that the recipe does not recreate. Candidates, in order of suspicion: manual_edits.json no longer holds every hand edit that the live tree has (apply reports 973/1042 and 33/1042 on first pass, and the ones it cannot place are silently lost); find_icall_esp_saves only re-fixes four named functions, so any other icall_esp save point present in the live tree is gone; the 214 hand-written guards referenced in seed_missing_functions' own docstring may not all be represented. The live tree is therefore the only copy of some fixes, which is exactly why it measures better and why it cannot be rebuilt. STOP REGENERATING UNTIL THIS IS CLOSED. The next step is a diff, not a change: regenerate to scratch, run the recipe, and compare the result against the live tree function by function to enumerate precisely what the recipe fails to restore. That list is the missing piece of the pipeline. Until then every lifter improvement is unlandable, because landing one requires a regeneration that costs 22 kernel calls. The fragment promotion itself is committed (efa8fce) and verified at the generation step; it is correct and waiting. Baseline restored and verified at 54/4/2/8.",
    "commit": "efa8fce"
  },
  {
    "kernel_calls": 54,
    "failed_icalls": 4,
    "total_icalls": 60275,
    "distinct_ordinals": 15,
    "ended": "crash",
    "heap_allocs": 2,
    "crash_in": "sub_002235D0+0xEA",
    "fault_va": "0xFFFFFF88",
    "date": "2026-08-05",
    "message": "FOUND IT: manual_edits.py places only 68 of 153 edits against the tree it was extracted from",
    "note": "Direct test of the round-trip hypothesis, no regeneration needed. Ran 'manual_edits.py apply --partial --force' against the LIVE 54/4/2/8 tree - the same tree the store was extracted from, so every anchor should match by construction. It placed 68/153. Eighty-five edits cannot be re-applied even to their own source. That is the ~22 kernel calls: every regeneration silently drops them, which is why two unrelated changes both landed on exactly 32/13/3/8. The store is not incomplete - it has 153 entries against ~60 in-tree markers plus 73 'moved to' stub replacements, which is proportionate. The defect is in APPLY, not in EXTRACT. The anchors are single source lines, and after a regeneration - or even after an earlier edit in the same file shifts context - they no longer match uniquely, so the edit is dropped. --force then writes the partial result anyway and reports success per file, which is how this stayed invisible: the failure is per-edit, the reporting is per-file. WHAT TO FIX, in order: (1) apply must fail loudly and refuse to write when it cannot place an edit, rather than writing a partial tree under --force; (2) anchors need to be more robust than one exact line - function name plus an offset or a small context window; (3) the 85 that cannot place need enumerating individually so each is either re-anchored or re-derived. That list is now a concrete worklist rather than a mystery. ALSO NOTE: the diagnostic run itself wrote to the live tree under --force and had to be rolled back from a snapshot - apply is destructive even when it fails, which is its own argument for fix (1). Baseline restored and re-verified at 54/4/2/8.",
    "commit": "efa8fce"
  },
  {
    "kernel_calls": 54,
    "failed_icalls": 4,
    "total_icalls": 60275,
    "distinct_ordinals": 15,
    "ended": "crash",
    "heap_allocs": 2,
    "crash_in": "sub_002235D0+0xEA",
    "fault_va": "0xFFFFFF88",
    "date": "2026-08-05",
    "message": "RETRACTION: the 68/153 result does not mean 85 edits are lost",
    "note": "Correcting the previous entry, which recorded a wrong conclusion. I ran 'manual_edits.py apply' against the already-applied live tree, got 68/153, and called it proof that 85 hand fixes are silently dropped on every regeneration. That inference does not hold. READING THE TOOL PROPERLY: replace_line has an explicit 'already applied is success, not failure' branch. insert_before has one too, and it is POSITIONAL rather than a whole-file text search, with a comment explaining why - many guards are worded identically, so a text search would make every duplicate look already-applied and silently drop it. wrap locates by the whole enclosed run rather than the anchor line, for the same reason. This is careful, well-reasoned code and I mischaracterised it. WHAT 68/153 ACTUALLY SHOWS: applying to a tree that already carries the edits is not the round-trip test I claimed it was. The reported failures are 'anchor line not found in function', and the likely mechanism is narrow - _find_anchor disambiguates using preceding context, and once a block has been inserted the line before the anchor IS that block, so the context no longer matches and an edit that is correctly in place reports as failed. That is a reporting artefact of re-running, not evidence of loss. WHAT IS STILL TRUE AND STILL UNEXPLAINED: two unrelated changes both regenerated to exactly 32/13/3/8 against a 54/4/2/8 baseline. Something in the recipe does not restore the live tree. But manual_edits is no longer the identified culprit - it is back to being one candidate among several, alongside find_icall_esp_saves only re-fixing four named functions. THE TEST THAT WOULD ACTUALLY SETTLE IT, and which I have not run: regenerate to scratch, run the full recipe, then diff the result against the live tree function by function. The differences ARE the loss. Anything short of that diff is inference. Also worth noting: apply --force wrote a partial tree during the diagnostic and I had to roll back from a snapshot. That behaviour is deliberate and documented in the source - it preserves ~100 good guards rather than discarding a whole file - but it does mean the tool is destructive on a re-run, which is worth a guard. Baseline restored and verified at 54/4/2/8.",
    "commit": "efa8fce"
  },
  {
    "kernel_calls": 54,
    "failed_icalls": 4,
    "total_icalls": 60275,
    "distinct_ordinals": 15,
    "ended": "crash",
    "heap_allocs": 2,
    "crash_in": "sub_002235D0+0xEA",
    "fault_va": "0xFFFFFF88",
    "date": "2026-08-05",
    "message": "ROOT CAUSE of the 22-call regeneration loss: 35 XAPI functions get renamed, breaking hand-written references",
    "note": "Ran the diff instead of theorising. Saved the working tree, regenerated + full recipe, saved that, restored, compared function by function. Good: 29,869 functions. Rebuilt: 29,824. FIFTY functions exist only in the good tree, and they are contiguous in the XAPI range - sub_0019CC92, sub_0019CEDD, sub_0019EAC2, sub_0019EB69, sub_0019EEC8, sub_0019EF00, sub_0019F038, sub_0019F22E and so on. Rebuilt has 35 definitions named XAPILIB_* (XAPILIB_XapiSelectCachePartition, XAPILIB_XMountUtilityDrive, XAPILIB_XCalculateSignatureBegin, XAPILIB_XLaunchNewImageA, ...); good has ZERO. So the regeneration emits these functions under the XbSymbolDatabase names carried in seeded_functions.json's name field, while the working tree has them as sub_ADDRESS. Every hand-written reference by sub_ name - main.c, recomp_manual.c's recomp_lookup_manual overrides, the seeded registrations - then points somewhere else or nowhere. sub_0019F22E is directly on the boot path (xbe_entry_point calls it), and sub_0019EB69 is the CRT boot code main.c pre-seeds 0x44B0D8 for. That is the 22 kernel calls. THE FIX ALREADY HALF EXISTS: normalise_seed_names.py rewrites XAPILIB_* to sub_ADDRESS, but only inside recomp_seed.c. It needs to run across the whole generated tree, or the generator should emit sub_ADDRESS unconditionally and keep the readable names as comments. The second is cleaner - one naming scheme everywhere, readable names still available in the JSON. 1,329 functions differ in body between the two trees, which is expected: fragment promotion and the fs: change both landed in the rebuild. Those are not implicated. WHAT THIS UNBLOCKS: with consistent naming, a regeneration should reproduce the baseline, and then fs: segments, thread-local registers and fragment promotion all land together. Baseline restored and verified at 54/4/2/8.",
    "commit": "92a8e98"
  }
]